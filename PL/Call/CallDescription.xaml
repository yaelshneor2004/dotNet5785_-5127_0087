<Window x:Class="PL.Call.CallDescription"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CallDescription" Height="450" Width="800"
                DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
         xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
        Language="he-IL" Loaded="VolunteerListWindow_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Label Content="Call Description:" VerticalAlignment="Center"/>
            <Label Content="{Binding Description, Mode=TwoWay}" Width="636" Height="74"/>
        </StackPanel>
        
        <Grid Grid.Column="2" Grid.Row="1" MinWidth="400" Margin="20" Visibility="{Binding CurrentVolunteer.IsActive, Converter={StaticResource ActiveCallToVisibleConverterKey}}">
            <wv2:WebView2 Name="MapWebView"
          Source="{Binding MapSource}" 
          HorizontalAlignment="Stretch" 
          VerticalAlignment="Stretch"/>
        </Grid>
    </Grid>

</Window>
