<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Volunteer"
        mc:Ignorable="d"
        Title="VolunteerWindow" Height="600" Width="450"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            
            <Label Content="ID:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.Id,Mode=TwoWay}" IsReadOnly="{Binding CurrentVolunteer.Id, Converter={StaticResource ConvertObjIdToTFKey}}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Full Name:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.FullName}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Phone:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.Phone}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Email:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.Email}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Password:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.Password}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Address:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.Address}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Role:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource SortCollectionRolesKey}}"
                      SelectedItem="{Binding Role}"
                      SelectedValue="{Binding CurrentVolunteer.Role, Mode=TwoWay}"
                      Style="{StaticResource {x:Type ComboBox}}"
                      Foreground="Black"
                      IsReadOnly="{Binding CurrentVolunteer.Role, Converter={StaticResource ConvertRoleToTFKey}}"/>

            <Label Content="Is Active:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <CheckBox IsChecked="{Binding CurrentVolunteer.IsActive, Mode=OneWay}" Style="{StaticResource {x:Type CheckBox}}"/>

            <Label Content="Max Distance:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <TextBox Text="{Binding CurrentVolunteer.MaxDistance}" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

            <Label Content="Type Distance:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
            <ComboBox ItemsSource="{Binding Source={StaticResource SortCollectionDistancesKey}}" SelectedItem="{Binding TypeDistance}" SelectedValue="{Binding CurrentVolunteer.TypeDistance, Mode=TwoWay}" Style="{StaticResource {x:Type ComboBox}}" Foreground="Black"/>

            <!-- New Fields Panel -->
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Height="Auto" Margin="13,0,0,0" Width="320">
                <Label Content="Call Type:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource SortCollectionMyCallTypeKey}}" SelectedItem="{Binding CallType}" SelectedValue="{Binding CurrentVolunteer.CurrentCall.CallType, Mode=TwoWay}" IsEnabled="False" Style="{StaticResource {x:Type ComboBox}}" Foreground="Black"/>

                <Label Content="Description:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <TextBox Text="{Binding CurrentVolunteer.CurrentCall.Description}" IsReadOnly="True" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

                <Label Content="Max End Time:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <TextBox Text="{Binding CurrentVolunteer.CurrentCall.MaxEndTime}" IsReadOnly="True" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

                <Label Content="Call Address:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <TextBox Text="{Binding CurrentVolunteer.CurrentCall.Address}" IsReadOnly="True" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

                <Label Content="Status:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <ComboBox ItemsSource="{Binding Source={StaticResource SortCollectionCallStatusByVolunteerKey}}" SelectedItem="{Binding Status}" SelectedValue="{Binding CurrentVolunteer.CurrentCall.Status, Mode=TwoWay}" IsEnabled="False" Style="{StaticResource {x:Type ComboBox}}" Foreground="Black"/>

                <Label Content="Start Time:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <TextBox Text="{Binding CurrentVolunteer.CurrentCall.StartTime}" IsReadOnly="True" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>

                <Label Content="Distance From Volunteer:" Style="{StaticResource {x:Type Label}}" Foreground="Blue"/>
                <TextBox Text="{Binding CurrentVolunteer.CurrentCall.DistanceFromVolunteer}" IsReadOnly="True" Style="{StaticResource {x:Type TextBox}}" Foreground="Black"/>
            </StackPanel>

            <Button Content="{Binding ButtonText}" Width="255" Click="btnAddUpdate_Click" HorizontalAlignment="Center" Margin="0,23,0,43"/>
        </StackPanel>
    </ScrollViewer>
</Window>
