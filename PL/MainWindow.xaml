<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="MainWindow_Loaded" 
        Closed="MainWindow_Closed"
        Language="he-IL">



    <!--grid in all the window-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Margin="-27,29,67,11">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border BorderBrush="#9370DB" BorderThickness="2" Background="White" Padding="0" HorizontalAlignment="Center" CornerRadius="10">
                <Label Content="{Binding CurrentTime, StringFormat=MM/dd/yyyy}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="100" Margin="0" FontWeight="Bold" RenderTransformOrigin="1.214,0.574"/>
            </Border>

            <Button Content="Add One Minute" HorizontalAlignment="Left" Height="41" Margin="110,0,0,0" VerticalAlignment="Center" Width="150" Click="btnAddOneMinute_Click" Grid.Row="1"/>
            <Button Content="Add One Hour" HorizontalAlignment="Left" Height="41" Margin="110,0,0,0" VerticalAlignment="Center" Width="150" Click="btnAddOneHour_Click" Grid.Row="2"/>
            <Button Content="Add One Day" HorizontalAlignment="Left" Height="40" Margin="110,0,0,0" VerticalAlignment="Center" Width="150" Click="btnAddOneDay_Click" Grid.Row="3"/>
            <Button Content="Add One Month" HorizontalAlignment="Left" Height="41" Margin="110,0,0,0" VerticalAlignment="Center" Width="150" Click="btnAddOneMonth_Click" Grid.Row="4"/>
            <Button Content="Add One Year" HorizontalAlignment="Left" Height="41" Margin="110,0,0,0" VerticalAlignment="Center" Width="150" Click="btnAddOneYear_Click" Grid.Row="5"/>
        </Grid>
        <!-- Updated Grid for DataGrid display on the top right -->
        <Grid Grid.Row="0" Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Content="Status Quantities" HorizontalAlignment="Left" FontSize="15" Margin="130,13,0,7" Grid.Row="0" FontWeight="Bold"/>
            <DataGrid  AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding StatusList, Mode=TwoWay}"  Background="Transparent" MouseLeftButtonUp="DataGrid_MouseLeftButtonUp" Margin="130,43,9,145" Grid.RowSpan="2" SelectedItem="{Binding selectedValue,Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding value}" Header="Status" Width="*"/>
                    <DataGridTextColumn Binding="{Binding index}" Header="Quantity" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>


        <Grid Grid.Row="0" Margin="240,40,200,0" Grid.ColumnSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Init DB" HorizontalAlignment="Center" Height="50" Width="150" Click="btnInitDB_Click" Grid.Row="0"/>
            <Button Content="Reset DB" HorizontalAlignment="Center" Height="50" Width="150" Click="btnResetDB_Click" Grid.Row="1"/>
            <Button Content="Handle Volunteers" HorizontalAlignment="Center" Height="50" Width="150" Click="btnHandleVolunteers_Click" Grid.Row="2"/>
            <Button Content="Handle Calls" HorizontalAlignment="Center" Height="50" Width="150" Click="btnHandleCalls_Click" Grid.Row="3"/>
        </Grid>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Max Range-[Maximum time for an open call After this time the call will expire.]" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Column="0" Margin="10,2,0,0" Grid.Row="0"/>
            <TextBox Text="{Binding MaxRiskRange}" Grid.Column="0" Margin="10,0,0,0" Grid.Row="1"/>
            <Button Content="Update" HorizontalAlignment="Left" Width="112" Height="30" Click="btnUpdate_Click" Grid.Column="1" Margin="10,0,0,1" Grid.Row="1"/>
        </Grid>
    </Grid>
</Window>
